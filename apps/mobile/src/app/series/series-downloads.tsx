// EXPERIMENTAL series page twin of /series-downloads — the same screen, registered inside the modal's own
// stack (see _layout.tsx here) so it pushes as a real page over the combined series page.
// Remove with the experiment.
export { default } from '../series-downloads';
